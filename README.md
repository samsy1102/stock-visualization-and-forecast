DB = MongoDB

# for stocks.py
pip install streamlit
pip install yfinance
pip install statsmodels
pip install pandas
pip install numpy
pip install matplotlib

# for login.py
pip install hashlib
pip install re
pip install pymongo
